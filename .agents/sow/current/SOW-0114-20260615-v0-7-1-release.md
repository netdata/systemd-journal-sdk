# SOW-0114 - v0.7.1 Release

## Status

Status: in-progress

`completed` is the successful terminal status. `done` is a directory name, not a status value. Do not use `Status: done` or `Status: complete`.

Sub-state: activated on 2026-06-15 for a Rust/Go release with full Rust/Go
tests, benchmark evidence, crates.io publication, and root plus Go module tags.

## Requirements

### Purpose

Publish a consumable `v0.7.1` release for Netdata integration, using the
completed post-`0.7.0` Rust and Go work, while proving that Rust and Go tests
still pass and representative reader/writer benchmark results do not show an
obvious performance regression before publishing crates or pushing tags.

### User Request

The user agreed to release `0.7.1`, but requested the entire Rust and Go test
suite and benchmarks first to check whether recent work negatively influenced
performance.

### Assistant Understanding

Facts:

- `v0.7.1` and `go/v0.7.1` do not exist locally or remotely at activation time.
- Existing `v0.7.0` and `go/v0.7.0` tags both peel to
  `556e79e1e9eabab84becb5f6d0658b6f39ba7075`.
- `master` is ahead of `origin/master` with completed post-`0.7.0` work.
- Rust workspace and internal dependency versions are still `0.7.0`.
- Go module path is `github.com/netdata/systemd-journal-sdk/go`, so a Go
  subdirectory tag `go/v0.7.1` is required for Go consumers.
- The SOW-0097/SOW-0098 planning refresh was committed separately before this
  SOW as `29b1581`.

Inferences:

- This release should follow the SOW-0110 pattern: bump Rust versions and docs
  snippets, validate, publish Rust crates, push `master`, then create/push both
  annotated tags on the release commit.
- Because the user explicitly requested performance assurance, publish and tag
  steps must wait until full Rust/Go tests and Rust/Go reader/writer benchmark
  runs complete and their results are recorded.

Unknowns:

- Whether crates.io accepts all `0.7.1` package publishes without index
  propagation delay.
- Whether benchmark noise on the workstation is low enough to make a strong
  performance conclusion; if not, record the benchmark results as smoke/no
  obvious-regression evidence and do not overclaim.

### Acceptance Criteria

- Rust workspace package version and publishable internal dependency versions
  are `0.7.1`.
- User-facing Rust/Go install snippets that name `0.7.0` are updated to
  `0.7.1`.
- Full Go test suite passes.
- Full Rust workspace test suite passes.
- Rust/Go reader and writer benchmark runs complete and results are recorded
  with any caveats about noise or baseline comparability.
- Rust package dry-runs pass for all publishable Rust packages in dependency
  order.
- Rust crates are published to crates.io at `0.7.1`.
- `master` is pushed.
- Annotated tags `v0.7.1` and `go/v0.7.1` exist locally and remotely and peel
  to the same release commit.
- SOW audit and whitespace checks pass.

## Analysis

Sources checked:

- `.agents/skills/project-release-tagging/SKILL.md`.
- `.agents/skills/project-agent-orchestration/SKILL.md`.
- `go/go.mod`.
- `rust/Cargo.toml`.
- `tests/benchmarks/run_reader_core_benchmarks.py --help`.
- `tests/benchmarks/run_writer_core_benchmarks.py --help`.
- `.agents/sow/done/SOW-0110-20260614-v0-7-0-release.md`.

Current state:

- `master` is ahead of `origin/master`; this release must push those commits
  before tags.
- Working tree was clean after committing SOW-0097/SOW-0098 refresh.
- Rust version strings and docs still name `0.7.0`.
- `v0.7.1` and `go/v0.7.1` are available.

Risks:

- Crate publication and tag pushes are durable. If validation fails, do not
  publish or tag until fixed.
- Benchmarks can be noisy. Treat benchmark results as release evidence, not as
  absolute performance proof unless the run is stable and comparable.
- Tag targets are effectively immutable for Go module consumers. Verify local
  and remote tag state before creation and after push.

## Pre-Implementation Gate

Status: ready

Problem / root-cause model:

- Netdata integration needs a stable SDK version after post-`0.7.0` Rust/Go
  work. The repository has releaseable changes, but Rust crates and Go tags
  still identify the last published `0.7.0` version.

Evidence reviewed:

- `git tag -l 'v0.7.1' 'go/v0.7.1'`: no local tags.
- `git ls-remote --tags origin refs/tags/v0.7.1 refs/tags/v0.7.1^{} refs/tags/go/v0.7.1 refs/tags/go/v0.7.1^{}`:
  no remote tags.
- `go/go.mod`: module path is `github.com/netdata/systemd-journal-sdk/go`.
- `rust/Cargo.toml`: workspace package version and internal dependency
  versions are `0.7.0`.
- SOW-0110: existing release process and validation pattern for `0.7.0`.

Affected contracts and surfaces:

- Rust crates.io packages.
- Go module semantic version tags.
- README and docs installation snippets.
- SOW status ledgers.

Existing patterns to reuse:

- SOW-0110 release preparation and closure pattern.
- Project release-tagging skill requirement for matching root and `go/` tags.
- Existing benchmark harnesses under `tests/benchmarks/`.

Risk and blast radius:

- Medium release/process risk because crates.io package versions and git tags
  cannot be casually changed after publication.
- Low code-change risk: release preparation should only update version strings,
  docs snippets, lockfile metadata, and SOW release records.
- Medium validation duration: full Rust/Go tests and benchmarks may take time.

Sensitive data handling plan:

- Do not record crates.io tokens, cargo credential paths, environment values, or
  other secret material. Durable SOW evidence may include package names,
  versions, command names, aggregate benchmark rates, and tag hashes.

Implementation plan:

1. Create this active release SOW and update SOW status ledgers.
2. Bump Rust workspace/internal dependency versions and documentation snippets
   from `0.7.0` to `0.7.1`; update `rust/Cargo.lock`.
3. Run full Rust and Go tests.
4. Run Rust/Go reader and writer benchmark suites and record aggregate results.
5. Run Rust package publish dry-runs in dependency order.
6. Commit release preparation and SOW validation evidence.
7. Publish Rust crates, push `master`, create/push annotated `v0.7.1` and
   `go/v0.7.1` tags, and verify remote peeled targets.
8. Close the SOW by moving it to done with publication/tag evidence in the same
   final release-close commit.

Validation plan:

- `cd go && GOCACHE="$PWD/.local/go-build" GOMODCACHE="$PWD/.local/go-mod-cache" go test ./...`.
- `CARGO_HOME="$PWD/.local/cargo" CARGO_TARGET_DIR="$PWD/.local/cargo-target" cargo test --manifest-path rust/Cargo.toml --workspace`.
- `python3 tests/benchmarks/run_reader_core_benchmarks.py --languages rust,go,systemd --rows 100000 --directory-rows 100000 --repetitions 3 --warmups 1 --out .local/benchmarks/sow-0114-reader`.
- `python3 tests/benchmarks/run_writer_core_benchmarks.py --languages rust go systemd --rows 100000 --repetitions 3 --warmups 1 --output-dir .local/benchmarks/sow-0114-writer`.
- `cargo publish --dry-run` for all seven publishable Rust packages in
  dependency order.
- `python3 tests/docs/check_wiki_docs.py`.
- `python3 tests/docs/verify_examples.py`.
- `git diff --check`.
- `.agents/sow/audit.sh`.
- Remote tag verification after push.

Artifact impact plan:

- AGENTS.md: no update expected; release workflow already covered.
- Runtime project skills: update `project-release-tagging` if the crates.io
  dependency-order dry-run constraint recurs.
- Specs: no behavior spec update expected.
- End-user/operator docs: update install snippets from `0.7.0` to `0.7.1`.
- End-user/operator skills: no update expected.
- SOW lifecycle: this SOW is active in `current/`; close as `completed` and
  move to `done/` after release publication and tag verification.
- SOW-status.md: update canonical and root ledgers on activation and closure.

Open-source reference evidence:

- No external OSS reference is needed; this is a repository release process.

Open decisions:

- Resolved by user on 2026-06-15: release `0.7.1`, but run full Rust/Go tests
  and benchmarks before publish/tag actions.

## Implications And Decisions

1. 2026-06-15 release scope.
   - Decision: release `0.7.1` for Rust and Go consumers.
   - Implication: Rust crates.io publication and root plus Go submodule tags
     are part of the release.
   - Risk: publication and tag pushes are durable; validation must pass first.

2. 2026-06-15 validation scope.
   - Decision: run the entire Rust and Go test suites plus Rust/Go reader and
     writer benchmark runs before publish/tag actions.
   - Implication: release takes longer, but gives better confidence that recent
     changes did not obviously regress performance.
   - Risk: benchmark noise may require cautious interpretation.

## Plan

1. Prepare version/docs/lockfile changes and release SOW status.
2. Run full Rust/Go tests.
3. Run Rust/Go reader and writer benchmark suites.
4. Run package dry-runs and repository audits. For crates with unpublished
   internal `0.7.1` dependencies, dry-run and publish one crate at a time in
   dependency order after the release-prep commit.
5. Commit release preparation.
6. Publish crates, push branch, create/push tags, verify tags.
7. Close SOW with release evidence.

## Delegation Plan

Implementer:

- Local implementation by the project manager. This is a release mechanics SOW,
  not a code implementation SOW.

Reviewers:

- No external reviewer batch unless validation exposes a design, security, or
  production-grade concern. Release correctness is checked by full tests,
  benchmark evidence, package dry-runs, SOW audit, and tag verification.

Repository boundary block for every external-agent prompt:

```text
CRITICAL REPOSITORY BOUNDARY:
- Do not make changes outside this repository for any reason.
- Repository path: current repository root.
- You may inspect external references read-only when the task requires it.
- Write, edit, delete, move, reset, checkout, install, generate, cache, or format nothing outside this repository.
- The only write exception outside the repository is /tmp.
- Prefer .local/ inside this repository for scratch work, generated temporary files, cloned references, logs, and working notes.
```

Failure handling:

- If tests, benchmarks, dry-runs, audit, publish, push, or tag verification
  fail, record the failure in this SOW and stop before irreversible publish/tag
  actions unless the failure occurs after publication and must be repaired with
  a follow-up release.
- If either `v0.7.1` tag already appears or points to a different commit, stop
  and ask the user before any tag action.

## Execution Log

### 2026-06-15

- Activated this release SOW after committing the SOW-0097/SOW-0098 planning
  refresh separately as `29b1581`.
- Updated Rust workspace/internal dependency versions and public install
  snippets from `0.7.0` to `0.7.1`.
- Full Go test suite passed with `go test ./...`.
- Full Rust workspace test suite passed with `cargo test --workspace`.
- Reader core benchmark completed for Rust, Go, and systemd at 100k rows, 100k
  directory rows, 3 measured repetitions, 1 warmup.
- Writer core benchmark completed for Rust, Go, and systemd at 100k rows, 3
  measured repetitions, 1 warmup. The first writer run produced valid `PASS`
  JSON but a misleading stdout `fail` line because the summary helper checked
  lowercase `ok` against uppercase `PASS`; fixed the stdout helper in the
  writer-core and writer-directory harnesses.
- Created a temporary `v0.7.0` clone under `/tmp` and ran matching reader and
  writer benchmark baselines for same-machine comparison.
- Re-ran current Rust writer-core with 5 measured repetitions plus systemd
  control after the first current writer median looked noisy.
- Updated `.agents/skills/project-release-tagging/SKILL.md` to record the
  crates.io dependency-order dry-run constraint already observed in SOW-0110.
- `cargo publish --dry-run --allow-dirty` passed for
  `systemd-journal-sdk-common` at `0.7.1`.
- Dependent crate dry-run for `systemd-journal-sdk-registry` failed as expected
  because `systemd-journal-sdk-common 0.7.1` is not in the crates.io index yet.
  This matches SOW-0110: dependent crates must be dry-run immediately before
  publishing each crate after the previous dependency has been published.

## Validation

Acceptance criteria evidence:

- Rust workspace package version and publishable internal dependency versions
  are `0.7.1` in `rust/Cargo.toml`.
- Public install snippets under README/docs now reference `0.7.1`.
- Go module path still requires both `v0.7.1` and `go/v0.7.1` tags.
- Crate publication and tag evidence are pending until the release-prep commit
  is created and crates/tags are published.

Tests or equivalent validation:

- `go test ./...` under `go/` passed.
- `cargo test --manifest-path rust/Cargo.toml --workspace` passed.
- Reader core benchmark current run:
  `.local/benchmarks/sow-0114-reader/20260615T183038Z/summary.json`.
- Writer core benchmark current run:
  `.local/benchmarks/sow-0114-writer/compact-none-fss-off-api-raw-payload-live-every-1-mmap-windowed-20260615T183419288784Z/report.json`.
- Writer core repeat current run:
  `.local/benchmarks/sow-0114-writer-repeat/compact-none-fss-off-api-raw-payload-live-every-1-mmap-windowed-20260615T184401541315Z/report.json`.
- Reader same-machine `v0.7.0` comparison: current Rust file SDK payload
  median improved from 1.56M to 2.13M rows/s; Rust open-files SDK payload
  median moved from 2.10M to 1.94M rows/s; Go file SDK payload median moved
  from 1.09M to 0.95M rows/s; Go open-files SDK payload median moved from
  1.06M to 0.97M rows/s. Systemd file-data control moved from 642k to 580k
  rows/s, so small reader deltas are interpreted cautiously.
- Writer same-machine `v0.7.0` comparison: Go median was stable at 58.3k vs
  58.2k rows/s. The first Rust median was noisy at 48.6k rows/s; the repeat
  run measured 65.7k rows/s vs `v0.7.0` at 67.9k rows/s, with systemd control
  stable at 35.3k to 36.2k rows/s.
- `python3 tests/docs/check_wiki_docs.py` passed, validating 17 wiki files.
- `python3 tests/docs/verify_examples.py` passed, 65 of 65 examples.
- `python3 -m unittest tests/benchmarks/test_report_benchmarks.py` passed, 15
  tests.
- `python3 -m py_compile` passed for the two updated writer benchmark scripts.
- Direct helper assertions passed for writer benchmark public status mapping.
- `cargo publish --dry-run --allow-dirty` passed for
  `systemd-journal-sdk-common 0.7.1`. Remaining crate dry-runs are gated on
  publishing prior internal dependencies and will run immediately before each
  publish.
- `git diff --check` passed before the release-prep commit.
- `.agents/sow/audit.sh` passed before the release-prep commit.

Real-use evidence:

- Pending crates.io publication and tag push.

Reviewer findings:

- No external reviewer batch. This is a release mechanics SOW; validation is
  through full Rust/Go tests, benchmark comparison, docs validation, package
  dry-runs, audit, and tag verification.

Same-failure scan:

- `rg` found no native Go `Benchmark*` tests, Rust `benches`, Criterion
  benches, or `#[bench]` sources; the repository benchmark suite is the
  Python harness under `tests/benchmarks/` plus Rust/Go helper binaries.
- `git diff v0.7.0..HEAD -- rust/src/crates/journal-log-writer
  rust/src/crates/journal-core rust/src/bin
  tests/benchmarks/run_writer_core_benchmarks.py` showed no Rust writer source
  changes between `v0.7.0` and the candidate, supporting the noisy-first-run
  interpretation for the writer regression-looking result.

Sensitive data gate:

- No secrets, tokens, credentials, customer data, host journal data, or raw
  sensitive data were written to durable artifacts. Benchmark artifacts are
  under `.local/`; durable SOW evidence records aggregate rates and relative
  paths only.

Artifact maintenance gate:

- AGENTS.md: no update needed; release and SOW guardrails already covered.
- Runtime project skills: updated `project-release-tagging` for the crates.io
  dependency-order dry-run constraint.
- Specs: no behavior spec update needed; this release prep changes versions,
  docs snippets, and validation tooling status output only.
- End-user/operator docs: updated install snippets from `0.7.0` to `0.7.1`.
- End-user/operator skills: no output/reference skills affected.
- SOW lifecycle: active SOW remains in `current/` until crates and tags are
  published, then will close as `completed` and move to `done/`.
- SOW-status.md: canonical and root ledgers updated on activation; closure
  update pending after publication/tag verification.

Specs update:

- No spec update needed; public behavior and data contracts are unchanged by
  this release prep.

Project skills update:

- Updated `.agents/skills/project-release-tagging/SKILL.md`.

End-user/operator docs update:

- Updated README and wiki install snippets to `0.7.1`.

End-user/operator skills update:

- No end-user/operator skill artifacts are affected.

Lessons:

- Repeat benchmark outliers before treating them as release blockers,
  especially when source diffs do not touch the measured path and a control
  implementation is stable.
- Cargo dependent crate dry-runs for a new internal version cannot all pass
  before the first dependency is published to crates.io; release workflow must
  dry-run and publish one package at a time in dependency order.

Follow-up mapping:

- No deferred release-blocking follow-up remains before the release-prep
  commit. Crate publication, branch push, tag push, and release closure remain
  in this active SOW.

## Outcome

Pending.

## Lessons Extracted

Pending.

## Followup

None yet.

## Regression Log

None yet.

Append regression entries here only after this SOW was completed or closed and later testing or use found broken behavior. Use a dated `## Regression - YYYY-MM-DD` heading at the end of the file. Never prepend regression content above the original SOW narrative.
